source "https://rubygems.org"

# Matches GitHub Pages' Jekyll version, themes (incl. jekyll-theme-primer) and
# plugins, so the rendered site looks identical to the GitHub Pages changelog.
gem "github-pages", group: :jekyll_plugins
